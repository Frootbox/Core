
<button type="submit" class="btn btn-{{ field.getConfig('ButtonType') | default('primary') }}">
    <span data-editable-element data-uid="{{ field.getUid('title') }}"><span>{{ field.getTitle }}</span></span>
</button>

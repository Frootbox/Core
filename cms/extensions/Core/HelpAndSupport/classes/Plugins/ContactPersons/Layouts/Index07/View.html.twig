{# config

title: Standard
variables:
    columns:
        label: Spaltenzahl
        type: int
        default: 4
    image_width:
        label: Bildbreite
        type: int
        default: 400
    image_height:
        label: Bildhöhe
        type: int
        default: 400
    showFullEmail:
        label: E-Mail Adresse zeigen
        type: bool
/config #}

{% set i = view.getViewhelper('Delegator', { object: plugin }) %}
{% set sp = view.getViewhelper('SimplePartials') %}

{% set colClass = 'col-6' %}

<div class="plugin Core HelpAndSupport ContactPersons Index07">

    <div class="container">
        <div class="row">
            <div class="col-12">

                <h2 class="first main-title" data-editable data-uid="{{ plugin.getUid('title') }}">{{ plugin.getTitle }}</h2>

                <div class="teaser" data-editable data-uid="{{ plugin.getUid('teaser') }}"></div>

                {{ sp.renderPartial('Partials/Category', { fromfile: __FILE__, colClass: colClass, category: i.getCategory(), heading: 3 }) | raw }}


            </div>
        </div>
    </div>

</div>
